window.calcGuideLayout=function(firstGuide,guideCount,targetStripper){let low=0,high=5;for(let i=0;i<100;i++){let mid=(low+high)/2,total=0;for(let g=1;g<=guideCount;g++)total+=firstGuide*Math.pow(1+mid,g-1);if(total<targetStripper)low=mid;else high=mid}let gf=(low+high)/2,rows=[],cum=0;for(let g=1;g<=guideCount;g++){let spacing=firstGuide*Math.pow(1+gf,g-1);cum+=spacing;rows.push({g,spacing,cum})}return{rows,actual:cum,diff:cum-targetStripper,growthFactor:1+gf}};
